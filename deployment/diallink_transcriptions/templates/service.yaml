apiVersion: v1
kind: Service
metadata:
  name: {{ include "diallink_transcriptions_lite.fullname" . }}
  labels:
    {{- include "diallink_transcriptions_lite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diallink_transcriptions_lite.selectorLabels" . | nindent 4 }}
