apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diallink_transcriptions_lite.fullname" . }}-test-connection"
  labels:
    {{- include "diallink_transcriptions_lite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diallink_transcriptions_lite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
